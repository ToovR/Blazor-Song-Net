@inherits PlayerAudioComponent

<audio id="playerAudio" preload="auto">
    @if (Track != null)
    {
        <source src="@Track.Path" type="audio/mpeg" />
    }
    Your browser does not support the audio element.
</audio>